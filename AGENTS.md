# shareclass-claude-code — 에이전트 작업 지침

<!-- git-sync-rule:v1 -->
## Git 동기화 규칙 (모든 작업 필수)

이 저장소는 **여러 대의 기기에서 작업**된다. 한쪽이 커밋하지 않고 방치되면 다른 기기의 최신 작업과 충돌하고, 어느 쪽이 최신인지 판별하는 비용이 커진다. 아래를 작업의 시작·종료 절차로 반드시 지킬 것.

### 작업 시작 전 — 최신화
1. `git fetch` 후 **원격과의 격차 + 워킹트리 상태**를 먼저 확인한다.
2. 워킹트리가 깨끗하고 뒤처져 있으면 → `git pull --ff-only`로 최신화한 뒤 작업 시작. 의존성 변경이 있으면 `npm ci`도 함께.
3. 워킹트리가 더러운데 뒤처져 있으면 → **pull하지 말 것.** 미커밋 내용이 무엇인지 먼저 파악해 사용자에게 보고하고 처리 방향을 확인한다.

### 작업 종료 시 — 올려두기
1. 타입체크·테스트를 돌려 **통과한 경우에만** 기본 브랜치에 push한다. (push가 자동 배포로 이어질 수 있음)
2. 검증 실패·미검증 변경은 `wip/<기기>-<날짜>` 형태의 작업 브랜치로 push하고 사용자에게 보고한다.
3. 커밋되지 않은 작업을 남기고 세션을 끝내지 않는다. 당장 병합이 어려우면 최소한 브랜치로 push해 원격에 백업한다.

### 커밋 범위
- **워킹트리 전체를 뭉쳐서 커밋하지 말 것.** 요청받은 변경만 골라 커밋한다. 같은 파일에 다른 작업이 섞여 있으면 임시 worktree에서 기본 브랜치 위로 cherry-pick하는 방식이 안전하다.
- **대용량 바이너리(dmg·zip·exe 등) 커밋 금지.** GitHub 100MB 제한 + 저장소 비대화. 배포 산출물은 GitHub Releases를 쓴다.
- `.env`·키·토큰이 스테이징에 포함됐는지 push 전 확인한다.
